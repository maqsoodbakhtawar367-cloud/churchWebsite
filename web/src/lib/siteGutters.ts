/**
 * Shared horizontal padding (navbar + main content) so left edges line up with the logo.
 * Change in one place only.
 */
export const SITE_HORIZONTAL_GUTTER =
  "px-4 min-[400px]:px-5 sm:px-6 md:px-8 lg:px-10 xl:px-12 2xl:px-16";
